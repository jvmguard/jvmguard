If you want to see how jvmguard behaves with multiple JVMs, you can launch the
"demo_console" executable to start a couple of JVMs that simulated activity
which is then measured by the jvmguards server.

Note that the jvmguard server must be started separately.